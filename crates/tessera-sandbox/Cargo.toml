[package]
name = "tessera-sandbox"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tessera-error = { path = "../tessera-error" }
tessera-manifest = { path = "../tessera-manifest" }
tessera-ui-schema = { path = "../tessera-ui-schema" }
